// @flow
/**
 * Take Action
 * @author mtownsend
 * @since Feb 2019
 * 
 * Handles the taking of items
 */
export default (item:string) => {
  return {
    message: `${item} darts out of your grasp. What?`
  };
};